# Server Configuration
VITE_API_BASE_URL=http://localhost:5000/api

# MongoDB Configuration (If connecting from a serverless function or backend)
# IMPORTANT: Never connect to MongoDB directly from the frontend React/Vite code for security.
# This variable is intended for your backend.
MONGODB_URI=mongodb+srv://<username>:<password>@cluster0.mongodb.net/hacktropica?retryWrites=true&w=majority

# App Environment
VITE_APP_ENV=development
